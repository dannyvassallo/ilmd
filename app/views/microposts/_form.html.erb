<%= form_for(@micropost) do |f| %>
  <% if @micropost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@micropost.errors.count, "error") %> prohibited this micropost from being saved:</h2>

      <ul>
      <% @micropost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label>Doctor's Name(s)</label><br>
    <%= f.text_field :drname %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
    <%= f.hidden_field :user_id %>
  <div class="actions">
    <%= f.submit ( f.object.new_record? ? "Add a Note" : "Update"), :class => "btn btn-default" %>
  </div>
<% end %>
